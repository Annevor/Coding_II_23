public class ShoppingCart01 {
    public static void main(String[] args) {
        // Declare and initialize String variables.  Do not initialize message yet.
        String custName = "BILLYBOB";
        String itemDesc = "a fresh Scrubdaddy, straight from the factory.";
        String message;
        
        
        // Assign the message variable 
        message = custName + " is scrambling to purchase " + itemDesc;
        
        // Print and run the code
        System.out.println(message);
    }
}
